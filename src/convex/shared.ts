/** Shared commerce constants — single source of truth for backend and UI. */

/** Toman shipping flat rate. */
export const SHIPPING_FLAT_RATE = 49_000;

/** Free shipping applies at this subtotal (Toman). */
export const FREE_SHIPPING_THRESHOLD = 500_000;

/** Admin dashboard low-stock alert threshold. */
export const LOW_STOCK_THRESHOLD = 5;
